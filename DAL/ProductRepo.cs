using DomainModel;

namespace DAL;
public class ProductRepo
{
    public void Create(Product model)
    {
        //using ctx = new AppContext()
        //ctx.Add(model)
        //ctx.saveChanges();
    }
}
